



```shell
#!/bin/bash

# 记录开始时间
START_TIME=$(date +%s)
BUILD_START=$(date "+%Y-%m-%d %H:%M:%S")

# 执行部署脚本
sudo /data/code/scripts/gedi-tec/gedi-tec-deploy-ui.sh ${env} ${branch} ${module}

# 记录部署结果
DEPLOY_STATUS=$?

# 测试是否在git仓库
if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
  echo "Not a git repository."
  exit 0
fi

# 获取当前分支
CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)

# 获取最近的提交信息
COMMIT_HASH=$(git log -n 1 --pretty=format:'%h')
COMMIT_TIME=$(git log -n 1 --pretty=format:'%ad' --date=format:'%m-%d %H:%M')
COMMIT_MSG=$(git log -n 1 --pretty=format:'%s' --no-merges)
COMMIT_AUTHOR=$(git log -n 1 --pretty=format:'%an')

# 计算构建耗时
END_TIME=$(date +%s)
DURATION=$((END_TIME - START_TIME))
MINUTES=$((DURATION / 60))
SECONDS=$((DURATION % 60))

if [ $MINUTES -gt 0 ]; then
  DURATION_TEXT="${MINUTES}分${SECONDS}秒"
else
  DURATION_TEXT="${SECONDS}秒"
fi

# 环境文本
if [ -z "$env" ]; then
  env="test"
fi

if [ "$env" = "test" ]; then
  ENV_TEXT="测试环境"
  ENV_COLOR="info"
elif [ "$env" = "main" ]; then
  ENV_TEXT="生产环境"
  ENV_COLOR="warning"
else
  ENV_TEXT="未知环境"
  ENV_COLOR="comment"
fi

# 构建状态
if [ $DEPLOY_STATUS -eq 0 ]; then
  STATUS_TEXT="✅ 构建成功"
  STATUS_COLOR="info"
else
  STATUS_TEXT="❌ 构建失败"
  STATUS_COLOR="warning"
fi

# 构建markdown消息内容
MESSAGE_JSON=$(cat << EOF
{
    "msgtype": "markdown",
    "markdown": {
        "content": "## 📦 【前端】技经造价平台-输电 ${STATUS_TEXT}\n\n> 环境：<font color=\"info\">${ENV_TEXT}</font>  |  分支：<font color=\"info\">${CURRENT_BRANCH}</font>\n> ${COMMIT_TIME} ${COMMIT_MSG}\n> 构建时间：${BUILD_START}  |  耗时：<font color=\"warning\">${DURATION_TEXT}</font>"
    }
}
EOF
)

# 发送通知
echo "${MESSAGE_JSON}" | iconv -f UTF-8 -t UTF-8 | \
 curl "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx" \
  -H "Content-Type: application/json; charset=utf-8" \
  --data-binary @-

echo ""
echo "通知已发送"

```

