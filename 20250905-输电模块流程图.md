> 提示词：

- 这是一个项目的原型图，页面是历史指标库，请你根据页面内容写一个流程图，格式为mermaid，另外表格的右边有一个操作栏部分，功能是编辑和删除。

- 根据这个流程图用一段文字简单描述一下前端主要处理流程，描述成一段文字，简要概括即可，字数在250字以内

## 基础数据管理

> 在基础数据管理模块中，前端主要实现了左侧菜单与右侧 Tab 的切换逻辑，支持本体指标、其他指标、本体费用计算逻辑和费率基准四个页面的数据管理。整体流程涵盖查询、重置、表格渲染与刷新，以及新增、编辑、删除等表单弹窗操作；其中本体指标和其他指标额外支持导入、导出功能。前端负责表单校验、接口请求、文件上传下载和操作结果提示，保证数据管理的完整交互闭环。

```mermaid
flowchart TD

A[进入基础数据管理] --> B{左侧菜单}
B --> B1[指标基础信息管理]
B --> B2[基准数据管理]

%% 指标基础信息管理
B1 --> C{右侧Tab}
C --> C1[本体指标]
C --> C2[其他指标]

C1 --> C1_ops[功能: 查询 / 重置 / 新增 / 编辑 / 删除 / 导入 / 导出]
C2 --> C2_ops[功能: 查询 / 重置 / 新增 / 编辑 / 删除 / 导入 / 导出]

%% 基准数据管理
B2 --> D{右侧Tab}
D --> D1[本体费用计算逻辑]
D --> D2[费率基准]

D1 --> D1_ops[功能: 查询 / 重置 / 新增 / 编辑 / 删除]
D2 --> D2_ops[功能: 查询 / 重置 / 新增 / 编辑 / 删除]

```

## 历史指标库

> 

```mermaid
graph TD
    A[用户] --> B[进入历史指标库页面]
    B --> C{用户操作}
    C -->|查询| D[输入工程名称/线路段名称/工程编号等信息]
    D --> E[点击查询按钮]
    E --> F[显示查询结果]
    F --> C
    C -->|重置| G[清空查询条件]
    G --> F
    C -->|导入| H[选择文件导入]
    H --> I[显示导入结果]
    I --> F
    C -->|导出| J[选择导出格式]
    J --> K[下载导出文件]
    K --> F
    F --> L[显示操作栏]
    L --> M{操作栏功能}
    M -->|编辑| N[点击编辑按钮]
    N --> O[进入编辑模式]
    O --> P[修改信息]
    P --> Q[保存修改]
    Q --> F
    M -->|删除| R[点击删除按钮]
    R --> S[确认删除]
    S -->|是| T[删除记录]
    T --> F
    S -->|否| F
```

## 智能校审

用户进入智能辅助校审页面后，可以选择历史线路进行校审，或通过“添加线路工程”按钮新增线路。新增线路时，用户需填写相关信息并保存。在校审过程中，用户可以预览指标数据，进行校审，并保存结果。

```mermaid
graph TD
    A[用户] --> B[进入智能辅助校审页面]
    B --> C[查看历史线路数据库]
    C --> D[选择线路进行校审]
    D --> E[点击添加到校审基准库]
    E --> F[进入指标校审概览]
    F --> G[点击开始校审]
    G --> H[显示校审结果]
    H --> I[保存校审结果或关闭]
    B --> J[点击新增线路工程按钮]
    J --> K[填写线路工程信息]
    K --> L[点击保存]
    L --> B
    B --> M[点击指标预览按钮]
    M --> N[查看指标数据]
    N --> O[点击关闭或导出]
    O --> B
```

